export type Activity = { id: string; startTime: string; endTime: string; placeName: string; activity: string; durationMinutes: number; transportMode: string; transportMinutes: number; costNote: string; notes?: string };
export type TripPlan = { tripName: string; travelerSummary: string; pace: string; themes: string[]; timezone: string; budgetNote: string; days: Array<{ dayNumber: number; date?: string; title: string; activities: Activity[] }>; warnings: string[]; generatedBy: "codex" };
export type Trip = { id: string; title: string; state: string; updatedAt: string; activeRevision: { id: string; version: number; plan: TripPlan } | null; requirements?: unknown; requirementsRevision?: number };
export type Candidate = { providerPlaceId: string; displayName: string; latitude: number; longitude: number; category: string | null; sourceUrl: string; sourceType: "nominatim" | "ai_web" | "ai_knowledge" | "manual"; evidenceUrl: string | null; confidence: "high" | "medium" | "low"; decisionNote: string | null };
export type MapEntity = { id: string; activityId?: string | null; dayNumber: number; order: number; kind: "city" | "attraction" | "lodging" | "meal" | "stop" | "waypoint"; name: string; query: string; city: string; detail: string; importance: "primary" | "secondary" | "context"; startTime: string; endTime: string; durationMinutes: number; transportMode: string; costNote: string; notes: string; approximateLodgingArea: boolean; status: "pending" | "resolved" | "ambiguous" | "unresolved" | "failed"; location: Candidate | null; candidates: Candidate[]; warning: string | null };
export type MapRoute = { id: string; dayNumber: number; order: number; fromEntityId: string; toEntityId: string; mode: string; status: "pending" | "resolved" | "unresolved" | "failed"; geometry: GeoJSON.Geometry | null; warning: string | null };
export type MapDayPath = { dayNumber: number; entityIds: string[]; startEntityId: string; endEntityId: string; overnightEntityId: string };
export type MapSnapshot = { itineraryVersion: number; mapVersion: number; scope: "all" | "day"; dayNumber: number | null; status: "idle" | "queued" | "analyzing" | "resolving" | "ready" | "partial" | "failed" | "stopped"; summary: string; warnings: string[]; entities: MapEntity[]; routes: MapRoute[]; dayPaths: MapDayPath[] };
export type MapPatch = { tripId: string; itineraryVersion: number; mapVersion: number; entities: { upsert: MapEntity[]; remove: string[] }; routes: { upsert: MapRoute[]; remove: string[] }; dayPaths?: MapDayPath[] };
export type AiProgressEvent = { id: number; taskId: string; tripId: string; agent: "planner" | "map"; status: string; kind: string; summary: string; createdAt: string };
export type AiTask = { id: string; tripId: string; agent: "planner" | "map"; label: string; status: "starting" | "running" | "waiting" | "reconnecting" | "completed" | "failed" | "stopped"; summary: string; startedAt: string; updatedAt: string; canStop: boolean; events: AiProgressEvent[] };
