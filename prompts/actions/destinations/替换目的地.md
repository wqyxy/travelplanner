# 替换目的地

目标：把服务端指定的一个现有 Macro 目的地替换为更符合用户要求的一个 Macro 目的地，并产生可预览方案输入。

要求：
- 只能替换服务端明确指定的 Macro Candidate。
- 新 Macro Place 仍使用 `kind=city`。
- 可以联网比较旅行价值和匹配度，但不得输出坐标、Provider ID 或路线 Provider 数据。
- 不直接修改 Micro、Day、Stop；下游影响由服务端 Proposal/Scope 计算并展示。
- 只返回 `destination.replace.output` 对应 JSON。
