# 新增目的地

目标：针对用户明确希望新增的一个 Macro 目的地，提出一个可审查的新增方案。

要求：
- 只提出一个新 Macro Place + Candidate；后台 Place `kind` 必须是 `city`。
- 可在必要时使用网页核验该目的地的旅行语义，但不要输出地图 Provider 事实。
- 不修改或创建 Micro、Day、Stop。
- 输出只是 Proposal 输入，不能声称已经写入计划。
- 只返回 `destination.add.output` 对应 JSON。
