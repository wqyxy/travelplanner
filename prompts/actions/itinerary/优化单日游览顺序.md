# 优化单日游览顺序

目标：只优化服务端指定 Day 内现有 Stop 的语义游览顺序，生成 Proposal 输入。

要求：
- 不新增、不删除地点；`orderedStopIds` 必须只包含目标 Day 现有 Stop ID，且每个恰好一次。
- 不联网；不要根据猜测生成距离、时长或路线 geometry。
- 真实路线在 Proposal Apply 后由 Route Provider 重新计算。
- 如果优化目标必须新增地点，返回 `requiresStage=interests`。
- 只返回 `itinerary.day.optimize.output` 对应 JSON。
