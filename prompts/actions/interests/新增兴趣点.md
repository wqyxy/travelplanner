# 新增兴趣点

目标：针对用户明确想加入的一个具体地点，提出一个可审查的 Micro Candidate 新增方案。

要求：
- 必须绑定服务端指定的现有 Macro Candidate。
- Place kind 必须是现有非 `city` kind。
- 可在必要时联网核验地点实体和旅行价值；不要输出地图 Provider 事实。
- 不修改 Day/Stop，不声称已经写入计划。
- 只返回 `interest.add.output` 对应 JSON。
