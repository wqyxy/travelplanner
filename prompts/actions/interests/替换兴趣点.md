# 替换兴趣点

目标：把服务端指定的一个现有 Micro Candidate 替换为同一 Macro 下更符合用户要求的具体地点，并产生可预览方案输入。

要求：
- 新地点必须继续绑定同一个指定 Macro Candidate。
- 新 Place kind 必须是现有非 `city` kind。
- 可以联网比较地点价值，但不得输出坐标、Provider ID 或路线 Provider 数据。
- 不直接修改 Day/Stop；受影响行程由服务端 Scope/Proposal 计算。
- 只返回 `interest.replace.output` 对应 JSON。
