name: Phase 1 apply and verify

on:
  push:
    branches:
      - codex/final-route-phase1

permissions:
  contents: write

concurrency:
  group: final-route-phase1-apply
  cancel-in-progress: false

jobs:
  apply-and-verify:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Detect patch
        id: patch
        shell: bash
        run: |
          shopt -s nullglob
          parts=(.github/phase1.patch.part-*)
          if [[ -f .github/phase1.patch.gz.b64 || ${#parts[@]} -gt 0 ]]; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
          fi
      - uses: actions/setup-node@v4
        if: steps.patch.outputs.present == 'true'
        with:
          node-version: '24'
          cache: npm
      - name: Apply patch
        if: steps.patch.outputs.present == 'true'
        shell: bash
        run: |
          set -euo pipefail
          shopt -s nullglob
          parts=(.github/phase1.patch.part-*)
          if [[ -f .github/phase1.patch.gz.b64 ]]; then
            cp .github/phase1.patch.gz.b64 /tmp/phase1.patch.b64
          elif [[ ${#parts[@]} -gt 0 ]]; then
            printf '%s\n' "${parts[@]}" | sort | xargs cat > /tmp/phase1.patch.b64
          else
            exit 1
          fi
          base64 --decode /tmp/phase1.patch.b64 > /tmp/phase1.patch.gz
          gzip -dc /tmp/phase1.patch.gz > /tmp/phase1.patch
          git apply --check /tmp/phase1.patch
          git apply /tmp/phase1.patch
          rm -f .github/phase1.patch.gz.b64 .github/phase1.patch.part-*
      - name: Install dependencies
        if: steps.patch.outputs.present == 'true'
        run: npm ci
      - name: Typecheck
        if: steps.patch.outputs.present == 'true'
        run: npm run typecheck
      - name: Tests
        if: steps.patch.outputs.present == 'true'
        run: npm test
      - name: Production build
        if: steps.patch.outputs.present == 'true'
        run: npm run build
      - name: Commit verified Phase 1
        if: steps.patch.outputs.present == 'true'
        shell: bash
        run: |
          set -euo pipefail
          rm .github/workflows/phase1-apply-verify.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'feat: add final route foundation'
          git push origin HEAD:codex/final-route-phase1
