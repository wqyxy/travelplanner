name: Apply right-side single-entry workflow

on:
  push:
    branches:
      - impl/right-control-workflow

permissions:
  contents: write

concurrency:
  group: right-control-workflow
  cancel-in-progress: false

jobs:
  apply-and-verify:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm
      - name: Apply source transformations
        run: |
          python .github/apply-right-control.py
          python .github/fix-right-control.py
          python .github/fix-baseline-typecheck.py
      - name: Remove one-shot implementation helpers
        run: rm -f .github/apply-right-control.py .github/fix-right-control.py .github/fix-baseline-typecheck.py .github/workflows/apply-right-control.yml
      - name: Install dependencies
        run: npm ci
      - name: Typecheck
        run: npm run typecheck
      - name: Tests
        run: npm test
      - name: Production build
        run: npm run build
      - name: Commit verified implementation
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'feat: unify travel workflow in right control panel'
          git push origin HEAD:impl/right-control-workflow
